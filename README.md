# 🤖 Бот для автозапчастей

Telegram-бот для магазина автозапчастей с поиском, проверкой наличия и оформлением заказов.

## Возможности

✅ **Поиск запчастей:**
- По артикулу (точное совпадение)
- По названию (нечёткий поиск)
- По фото (отправка менеджеру)

✅ **Проверка наличия:**
- Показывает остаток на складе
- Цена для товаров в наличии
- Цена и срок под заказ

✅ **Оформление заказов:**
- Автоматический сбор контактов
- Уведомления администратору
- История заказов клиента

✅ **Админ-панель:**
- Добавление запчастей
- Просмотр заказов
- Статистика

## Установка

### 1. Создать бота в Telegram

Напишите [@BotFather](https://t.me/BotFather):
```
/newbot
Введите название: Автозапчасти Якутск
Введите username: avtozap_yakutia_bot
```

Скопируйте токен (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. Установить зависимости

```bash
npm install
```

### 3. Настроить конфигурацию

Откройте `bot.js` и замените:
```javascript
const TOKEN = 'ВАШ_ТОКЕН_ЗДЕСЬ'; // Токен от @BotFather
const ADMIN_ID = 'ВАШ_TELEGRAM_ID'; // Ваш ID (узнать у @userinfobot)
```

### 4. Запустить бота

```bash
npm start
```

Или для разработки:
```bash
npm run dev
```

## Заполнение базы данных

### Способ 1: Через админ-панель бота

Напишите боту `/admin` и следуйте инструкциям.

### Способ 2: Напрямую в SQLite

```bash
sqlite3 parts.db
```

```sql
-- Добавить категории
INSERT INTO categories (name, icon) VALUES 
('Тормозная система', '🔴'),
('Двигатель', '⚙️'),
('Подвеска', '🔧'),
('Кузовные детали', '🚗'),
('Электрика', '⚡');

-- Добавить запчасти
INSERT INTO parts (article, name, category, brand, price, availability, delivery_days, order_price, description) VALUES
('04465-30320', 'Тормозные колодки передние Camry 50', 'Тормозная система', 'Toyota', 2800, 5, 3, 2500, 'Оригинальные колодки для Toyota Camry 50, 55'),
('04465-33471', 'Тормозные колодки передние Land Cruiser 200', 'Тормозная система', 'Toyota', 4500, 0, 5, 4200, 'Передние колодки для LC 200, Lexus LX 570'),
('90919-02252', 'Катушка зажигания Corolla/Camry', 'Двигатель', 'Toyota', 3200, 3, 7, 2900, 'Оригинальная катушка зажигания 1ZZ/2AZ');
```

### Способ 3: Импорт из Excel

1. Сохраните прайс в CSV
2. Используйте скрипт:

```bash
node import-csv.js price.csv
```

## Использование

### Для клиентов

1. Найти бота по @username
2. Нажать START
3. Отправить артикул или название запчасти
4. Получить информацию о наличии и цене
5. Оформить заказ кнопкой "Купить" или "Заказать"

### Для администратора

Команды:
- `/admin` — панель управления
- `/stats` — статистика продаж
- `/orders` — список новых заказов

## Структура проекта

```
auto-parts-bot/
├── bot.js           # Основной файл бота
├── parts.db         # База данных SQLite
├── package.json     # Зависимости
└── README.md        # Этот файл
```

## Деплой на сервер

### Вариант 1: VPS (рекомендуется)

```bash
# Установка PM2 для постоянной работы
npm install -g pm2

# Запуск
pm2 start bot.js --name auto-parts-bot

# Автозапуск при перезагрузке
pm2 startup
pm2 save
```

### Вариант 2: Railway / Heroku

1. Загрузите код на GitHub
2. Подключите репозиторий к Railway
3. Добавьте переменную окружения `BOT_TOKEN`

## Настройка для Яндекс.Директа

Добавьте UTM-метки в ссылки:

```
https://t.me/avtozap_yakutia_bot?start=utm_yandex_direct
```

В боте отслеживайте:
```javascript
if (msg.text && msg.text.startsWith('/start utm_')) {
  const utm = msg.text.replace('/start ', '');
  // Сохранить в базу источник клиента
}
```

## Поддержка

По вопросам: [@ваш_username](https://t.me/ваш_username)
